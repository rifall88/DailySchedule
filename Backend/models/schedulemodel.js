// import db from "../config/database.js";

// export const getAllSchedules = (user, callback) => {
//   const sql = "SELECT * FROM schedule WHERE user_id = ?";
//   const values = [user_user.id];
//   db.query(sql, values, callback);
// };

// export const get
